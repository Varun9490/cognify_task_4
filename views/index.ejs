<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Complex Form Validation</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/styles.css" />
    <script defer src="/script.js"></script>
  </head>
  <body class="bg-gradient-to-r from-gray-900 to-gray-700 text-gray-200">
    <header class="bg-gray-800 text-white text-center py-6">
      <h1 class="text-3xl font-bold">Complex Form Validation</h1>
      <p class="text-xl mt-2">
        Enhance your form with advanced validation and dynamic interactions.
      </p>
    </header>

    <main class="container mx-auto my-8 px-4">
      <form id="complex-form" class="bg-gray-800 p-6 rounded-lg shadow-lg">
        <div class="mb-4">
          <label for="username" class="block text-lg font-medium"
            >Username</label
          >
          <input
            type="text"
            id="username"
            name="username"
            class="mt-1 p-2 w-full rounded-md border-gray-600 bg-gray-900 text-gray-100 focus:border-blue-500 focus:ring focus:ring-blue-500"
            required
          />
          <p id="username-error" class="text-red-500 text-sm mt-1 hidden">
            Username is required.
          </p>
        </div>

        <div class="mb-4">
          <label for="password" class="block text-lg font-medium"
            >Password</label
          >
          <input
            type="password"
            id="password"
            name="password"
            class="mt-1 p-2 w-full rounded-md border-gray-600 bg-gray-900 text-gray-100 focus:border-blue-500 focus:ring focus:ring-blue-500"
            required
          />
          <p id="password-error" class="text-red-500 text-sm mt-1 hidden">
            Password must be at least 8 characters long and contain a number.
          </p>
        </div>

        <button
          type="submit"
          class="bg-blue-500 text-white px-4 py-2 rounded-md hover:bg-blue-600 transition-transform transform hover:scale-105"
        >
          Submit
        </button>
      </form>

      <div id="message" class="mt-6"></div>
    </main>

    <footer class="bg-gray-800 text-white text-center py-4">
      <p>&copy; 2024 Complex Form Validation. All rights reserved.</p>
    </footer>
  </body>
</html>
